<div class="container">
	<div class="row-fluid">
		<span>
			<h1>Your day in <%= format(params[:area]) %>, Hong Kong</h1>
		</span>
	</div>
	<%= render @places %>
	<div class="row-fluid offset">
        <form id="submit">
            <button class="btn btn-large btn-block btn-danger start" onclick="BetaDialog.popDialog()">send plan to my email</button>
        </form>
	</div>
</div>